package org.example.study.baseSdk.chain;

/**
 * Placeholder result for future asynchronous chain submission.
 */
public record ChainSubmitResult(
        String taskId,
        String chainName,
        String status
) {
}
