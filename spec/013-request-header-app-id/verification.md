# 013 验证记录

执行命令：`mvn verify -q`，结果成功。

- 测试 32 个，失败 0 个，错误 0 个；2 个数据库集成测试按环境条件跳过。
- Checkstyle、SpotBugs 和 JaCoCo 覆盖率门槛全部通过。
- 签名请求进入检测接口、重放请求被拒绝、缺少鉴权头被拒绝的集成测试通过。
- Controller 测试确认同步和异步入口均把请求头参数传给应用服务。
