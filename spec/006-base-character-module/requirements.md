# 需求说明

## 背景

项目中会频繁使用分隔符、空白字符、括号、通配符等基础字符。如果这些字符散落在业务代码中，会降低可读性，也不利于后续沉淀 baseSdk 通用能力。

## 目标

- 在 baseSdk 中新增通用字符模块。
- 统一管理常用字符串常量和字符常量。
- 提供不可实例化的工具类，避免被错误注册成 Spring Bean。
- 保持模块轻量，不引入外部依赖。

## 非目标

- 不封装复杂字符串处理逻辑。
- 不替代 JDK、Spring、Apache Commons 等成熟字符串工具。
- 不在本需求中批量替换现有业务代码中的字符串字面量。
