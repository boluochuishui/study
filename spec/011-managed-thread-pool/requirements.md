# 011 通用线程池工厂需求

## 背景

检测任务将使用 `CompletableFuture` 异步执行，需要避免使用公共线程池，并为后续微服务提供一致的线程资源管理能力。

## 需求

- 在 `baseSdk` 中提供具名线程池工厂。
- 启动时从配置文件加载关键参数，未配置时使用默认值。
- 同名线程池只创建一个实例。
- 支持 MDC 上下文传递、拒绝日志、线程命名和优雅关闭。
- 提供运行状态快照，便于后续接入健康检查和监控。
- 第一版不支持运行时动态修改参数。
